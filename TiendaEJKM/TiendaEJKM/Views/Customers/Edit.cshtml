@model CustomersModel

@{
    ViewData["Title"] = "Editar cliente";
}

<h1>Editar cliente</h1>

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <form asp-action="Edit">
                @Html.AntiForgeryToken()
                <input asp-for="id_Customer" type="hidden" class="form-control" />
                <div class="mb-3">
                    <label asp-for="Name_Customer" class="form-label">Name_Customer</label>
                    <input asp-for="Name_Customer" type="text" class="form-control">
                </div>
                <div class="mb-3">
                    <label asp-for="Address_Customer" class="form-label">Address_Customer</label>
                    <input asp-for="Address_Customer" type="text" class="form-control">
                </div>
                <div class="mb-3">
                    <label asp-for="Phone_Customer" class="form-label">Phone_Customer</label>
                    <input asp-for="Phone_Customer" type="text" class="form-control">
                </div>
               
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Editar</button>

                    <a asp-action="Index">Regresar al listado</a>
                </div>
            </form>

        </div>
    </div>
</div>

@section Scripts {

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" href="//code.jquery.com/ui/1.13.0/themes/base/jquery-ui.css">
    <script src="https://code.jquery.com/ui/1.13.0/jquery-ui.js"></script>

    <script>
        function confirmAction() {
            if (confirm('¿Estás seguro de editar este cliente?')) {
                return true;
            } else {
                window.location.href = '@Url.Action("Index", "Customers")';
                return false;
            }
        }
    </script>
}